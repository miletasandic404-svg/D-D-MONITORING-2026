name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'npm'
        cache-dependency-path: |
          package-lock.json
          frontend/package-lock.json

    - name: Install backend dependencies
      run: npm ci

    # The root ESLint configuration loads the React Hooks plugin from the
    # frontend dependency tree, so install it before linting in a clean runner.
    - name: Install frontend dependencies
      run: cd frontend && npm ci

    - name: Run backend tests
      run: npm test

    - name: Run ESLint
      run: npx eslint .

    - name: Run frontend tests
      run: cd frontend && npm test

    - name: Build frontend
      run: cd frontend && npm run build
