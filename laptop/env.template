# D&D Monitoring — Laptop media node env (template)
# Kopiraj u C:\dnd-media\.env ili popuni direktno u start-laptop.bat

# Neon connection string. Prefer the restricted media_node_worker role
# (created by migration 029) via MEDIA_NODE_DATABASE_URL -- falls back to
# DATABASE_URL (owner role, bypasses RLS) if not set, for backwards
# compatibility, but the restricted role is strongly recommended.
DATABASE_URL=postgresql://USER:PASS@HOST/neondb?sslmode=require
# MEDIA_NODE_DATABASE_URL=postgresql://media_node_worker:PASS@HOST/neondb?sslmode=require

# REQUIRED for camera credential encryption/decryption to work at all.
# Must be the EXACT SAME 32-byte base64 value as Vercel's CREDENTIAL_ENCRYPTION_KEY
# env var -- if they differ, this node cannot decrypt passwords the cloud
# encrypted (or vice versa) and camera credentials will silently fail.
# Generate once with: openssl rand -base64 32
CREDENTIAL_ENCRYPTION_KEY=<isti-kao-na-vercelu>

# Iz registracije media node-a (korak 3 u laptop/README.md)
MEDIA_NODE_ID=<node-uuid>
MEDIA_NODE_HEARTBEAT_SECRET=<tajni-secret>

# Heartbeat endpoint
API_BASE_URL=https://www.dnd-monitoring.com/api

# Worker
CAMERA_SYNC_INTERVAL_SECONDS=60
MEDIAMTX_API_URL=http://127.0.0.1:9997
# MEDIAMTX_API_USER=           # samo ako ukljucis basic auth na 9997
# MEDIAMTX_API_PASS=

# ── Camera Setup Wizard V3: Cloudflare Tunnel (opciono) ──
# Potrebno za "Start Tunnel" dugme u wizardu. Prvo: cloudflared tunnel login,
# pa cloudflared tunnel create dnd-hls i cloudflared tunnel route dns dnd-hls hls.dnd-monitoring.com
CLOUDFLARE_TUNNEL_NAME=
# CLOUDFLARE_TUNNEL_CONFIG=C:\dnd-media\cloudflared\config.yml
